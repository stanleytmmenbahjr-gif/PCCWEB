# Example environment variables for the email backend
# Destination emails (comma-separated)
TO_EMAIL=pccliberia2025@gmail.com

# From address used in outgoing emails (must be allowed by SMTP provider)
FROM_EMAIL=no-reply@example.com

# Option 1: Send via SendGrid Web API
#SENDGRID_API_KEY=SG.xxxxx

# Option 2: SMTP transport
#SMTP_HOST=smtp.example.com
#SMTP_PORT=587
#SMTP_SECURE=false # true for 465, false for 587/TLS
#SMTP_USER=your-smtp-user
#SMTP_PASS=your-smtp-password

# Allowed origins for CORS (comma-separated)
CORS_ORIGIN=http://localhost:5173

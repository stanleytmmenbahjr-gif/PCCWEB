# Use the server-side email backend now. See `server/.env.example` for SMTP / SendGrid configuration.

# Development: optional fallback contact email for UI
VITE_CONTACT_EMAIL=info@pccliberia2025@gmail.com

# Base URL for backend API (optional). Defaults to http://localhost:3000
VITE_API_BASE=http://localhost:3000
